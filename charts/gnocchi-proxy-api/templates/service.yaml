apiVersion: v1
kind: Service
metadata:
  name: {{ include "gnocchi-proxy-api.fullname" . }}
  labels:
    {{- include "gnocchi-proxy-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "gnocchi-proxy-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
